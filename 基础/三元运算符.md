# C++ 三元运算符笔记

## 1. 什么是三元运算符

三元运算符（Ternary Operator）也叫**条件运算符**，是 C++ 中唯一的一个三目运算符。

### 基本语法

```cpp
条件 ? 表达式1 : 表达式2
```

### 语义

- 如果**条件为真**，返回**表达式1**的值
- 如果**条件为假**，返回**表达式2**的值

### 简单示例

```cpp
int a = 10, b = 20;
int max = (a > b) ? a : b;  // max = 20

// 等价于
int max;
if (a > b) {
    max = a;
} else {
    max = b;
}
```

---

## 2. 基本用法

### 2.1 返回值

```cpp
int x = 5;
int y = (x > 0) ? 1 : -1;  // y = 1
```

### 2.2 赋值

```cpp
int score = 85;
string result = (score >= 60) ? "Pass" : "Fail";  // "Pass"
```

### 2.3 函数参数

```cpp
int a = 10, b = 20;
printf("Max: %d\n", (a > b) ? a : b);
```

### 2.4 返回语句

```cpp
int getMax(int a, int b) {
    return (a > b) ? a : b;
}
```

### 2.5 输出

```cpp
int x = 5;
cout << "x is " << ((x > 0) ? "positive" : "negative") << endl;
```

---

## 3. 常见应用场景

### 3.1 求最大值/最小值

```cpp
int max = (a > b) ? a : b;
int min = (a < b) ? a : b;
```

### 3.2 绝对值

```cpp
int abs_value = (x >= 0) ? x : -x;
```

### 3.3 奇偶判断

```cpp
string parity = (n % 2 == 0) ? "Even" : "Odd";
```

### 3.4 符号判断

```cpp
int sign = (x > 0) ? 1 : (x < 0) ? -1 : 0;
```

### 3.5 边界检查

```cpp
int value = (input < 0) ? 0 : (input > 100) ? 100 : input;
// 限制 input 在 [0, 100] 范围内
```

### 3.6 默认值处理

```cpp
int result = (ptr != nullptr) ? ptr->value : 0;
```

### 3.7 字符串格式化

```cpp
cout << "You have " << count << " item" 
     << ((count == 1) ? "" : "s") << endl;
// 单数时：You have 1 item
// 复数时：You have 5 items
```

### 3.8 向上/向下取整

```cpp
// 向上取整
int ceil_div = (n % k == 0) ? n / k : n / k + 1;

// 等价于标准公式
int ceil_div = (n + k - 1) / k;
```

---

## 4. 嵌套三元运算符

### 4.1 多重条件判断

```cpp
int score = 85;
string grade = (score >= 90) ? "A" :
               (score >= 80) ? "B" :
               (score >= 70) ? "C" :
               (score >= 60) ? "D" : "F";
```

**等价的 if-else**：
```cpp
string grade;
if (score >= 90) {
    grade = "A";
} else if (score >= 80) {
    grade = "B";
} else if (score >= 70) {
    grade = "C";
} else if (score >= 60) {
    grade = "D";
} else {
    grade = "F";
}
```

### 4.2 符号函数（Sign Function）

```cpp
int sign = (x > 0) ? 1 : (x < 0) ? -1 : 0;
```

### 4.3 范围限制（Clamp）

```cpp
// 限制值在 [min, max] 范围内
int clamp = (value < min) ? min : (value > max) ? max : value;
```

### 4.4 多条件组合

```cpp
bool result = (a > 0 && b > 0) ? true : 
              (a < 0 && b < 0) ? true : false;

// 可以简化为
bool result = (a > 0 && b > 0) || (a < 0 && b < 0);
```

---

## 5. 优缺点分析

### 5.1 优点

#### ✅ 简洁
```cpp
// 三元运算符：1 行
int max = (a > b) ? a : b;

// if-else：5 行
int max;
if (a > b) {
    max = a;
} else {
    max = b;
}
```

#### ✅ 可读性好（简单情况下）
```cpp
// 清晰明了
string status = (age >= 18) ? "Adult" : "Minor";
```

#### ✅ 可以用在表达式中
```cpp
// 可以直接用在函数参数中
printf("Max: %d\n", (a > b) ? a : b);

// 可以用在初始化中
const int value = (condition) ? 10 : 20;
```

#### ✅ 性能可能更好
```cpp
// 三元运算符可能被编译器优化为条件移动指令（CMOV）
// 避免分支预测失败
```

### 5.2 缺点

#### ❌ 嵌套时可读性差
```cpp
// 难以阅读
int result = (a > b) ? ((c > d) ? 1 : 2) : ((e > f) ? 3 : 4);
```

#### ❌ 不适合复杂逻辑
```cpp
// 不推荐
int result = (condition) ? (doSomething(), value1) : (doOtherThing(), value2);

// 应该用 if-else
if (condition) {
    doSomething();
    result = value1;
} else {
    doOtherThing();
    result = value2;
}
```

#### ❌ 调试困难
```cpp
// 难以设置断点调试
int max = (a > b) ? a : b;

// if-else 可以分别在每个分支设置断点
if (a > b) {
    max = a;  // 断点
} else {
    max = b;  // 断点
}
```

---

## 6. 与 if-else 的对比

### 6.1 何时使用三元运算符

✅ **推荐使用**：
- 简单的条件赋值
- 单一表达式选择
- 代码简洁性优先
- 返回值选择

```cpp
// ✓ 推荐
int max = (a > b) ? a : b;
string result = (score >= 60) ? "Pass" : "Fail";
return (x > 0) ? x : -x;
```

❌ **不推荐使用**：
- 复杂的嵌套条件
- 需要执行多条语句
- 需要详细调试
- 可读性要求高

```cpp
// ✗ 不推荐
int result = (a > b && c > d) ? 
             (doSomething(), x + y) : 
             (doOtherThing(), x - y);
```

### 6.2 何时使用 if-else

✅ **推荐使用**：
- 复杂的条件逻辑
- 多条语句执行
- 需要详细注释
- 需要调试

```cpp
// ✓ 推荐用 if-else
if (condition) {
    // 复杂操作1
    // 复杂操作2
    result = value1;
} else {
    // 复杂操作3
    // 复杂操作4
    result = value2;
}
```

---

## 7. 性能考虑

### 7.1 分支预测

**三元运算符**：
```cpp
int max = (a > b) ? a : b;
// 可能被编译器优化为 CMOV 指令（条件移动）
// 避免分支预测
```

**if-else**：
```cpp
int max;
if (a > b) {
    max = a;
} else {
    max = b;
}
// 通常编译为分支指令（JMP）
// 依赖分支预测
```

### 7.2 现代编译器优化

**注意**：现代编译器（GCC、Clang、MSVC）会对简单的 if-else 进行优化，生成与三元运算符相同的代码。

```cpp
// 这两种写法在 -O2 优化下可能生成相同的汇编代码
int max1 = (a > b) ? a : b;

int max2;
if (a > b) max2 = a;
else max2 = b;
```

### 7.3 实际性能差异

在大多数情况下，性能差异**可以忽略不计**。选择时应该优先考虑**可读性和可维护性**。

---

## 8. 常见错误

### 8.1 忘记括号导致优先级问题

```cpp
// ❌ 错误
int result = condition ? a : b + c;  // 解析为 condition ? a : (b + c)

// ✓ 正确
int result = condition ? a : (b + c);

// 或者整个表达式加括号
int result = (condition ? a : b) + c;
```

### 8.2 类型不匹配

```cpp
// ⚠️ 警告：类型不一致
auto result = (condition) ? 1 : 2.5;  // result 类型是 double

// ✓ 明确类型
double result = (condition) ? 1.0 : 2.5;
```

### 8.3 嵌套过深

```cpp
// ❌ 难以阅读
int value = (a > b) ? ((c > d) ? ((e > f) ? 1 : 2) : 3) : 4;

// ✓ 使用 if-else 或拆分
int value;
if (a > b) {
    if (c > d) {
        value = (e > f) ? 1 : 2;
    } else {
        value = 3;
    }
} else {
    value = 4;
}
```

### 8.4 副作用（Side Effects）

```cpp
// ❌ 危险：表达式有副作用
int result = (condition) ? (++a) : (++b);

// ✓ 避免副作用
if (condition) {
    ++a;
    result = a;
} else {
    ++b;
    result = b;
}
```

### 8.5 返回类型不同

```cpp
// ❌ 编译错误：返回类型不兼容
auto result = (condition) ? "string" : 123;

// ✓ 确保类型兼容
string result = (condition) ? "string" : "123";
```

---

## 9. 高级用法

### 9.1 左值和右值

三元运算符可以返回左值：

```cpp
int a = 1, b = 2;
(a > b ? a : b) = 10;  // 修改较大的那个变量

cout << "a = " << a << ", b = " << b << endl;
// 输出：a = 1, b = 10
```

### 9.2 与逗号运算符结合

```cpp
int a = 1, b = 2;
int result = (a > b) ? (cout << "a is larger\n", a) : (cout << "b is larger\n", b);
// 先输出，再返回值
```

**注意**：不推荐这样写，可读性差。

### 9.3 constexpr 中使用

```cpp
constexpr int max(int a, int b) {
    return (a > b) ? a : b;
}

constexpr int value = max(10, 20);  // 编译时计算
```

### 9.4 模板中使用

```cpp
template<typename T>
T max(T a, T b) {
    return (a > b) ? a : b;
}
```

---

## 10. 代码风格建议

### 10.1 格式化

#### 单行格式（简单条件）
```cpp
int max = (a > b) ? a : b;
```

#### 多行格式（复杂条件）
```cpp
int result = (very_long_condition_expression) 
             ? value_when_true 
             : value_when_false;
```

#### 嵌套格式
```cpp
string grade = (score >= 90) ? "A" :
               (score >= 80) ? "B" :
               (score >= 70) ? "C" :
               (score >= 60) ? "D" : "F";
```

### 10.2 括号使用

```cpp
// ✓ 推荐：条件加括号
int result = (condition) ? value1 : value2;

// ✓ 也可以接受
int result = condition ? value1 : value2;
```

### 10.3 变量命名

```cpp
// ✓ 清晰的变量名
bool is_valid = (age >= 18);
string status = is_valid ? "Adult" : "Minor";

// ❌ 不清晰
auto x = (a > 18) ? "Adult" : "Minor";
```

---

## 11. 实战示例

### 11.1 判断闰年

```cpp
bool is_leap_year = (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
```

### 11.2 限制范围（Clamp）

```cpp
int clamp(int value, int min, int max) {
    return (value < min) ? min : (value > max) ? max : value;
}
```

### 11.3 交换变量（不推荐）

```cpp
// ✗ 不推荐：使用三元运算符交换
int temp = a;
a = b;
b = temp;

// ✓ 推荐：使用 std::swap
swap(a, b);
```

### 11.4 FizzBuzz（面试题）

```cpp
for (int i = 1; i <= 100; i++) {
    cout << ((i % 15 == 0) ? "FizzBuzz" :
             (i % 3 == 0)  ? "Fizz" :
             (i % 5 == 0)  ? "Buzz" : to_string(i)) << endl;
}
```

### 11.5 快速排序分区

```cpp
// 简化的分区逻辑
int partition(vector<int>& arr, int low, int high) {
    int pivot = arr[high];
    int i = low - 1;
    
    for (int j = low; j < high; j++) {
        if (arr[j] < pivot) {
            i++;
            swap(arr[i], arr[j]);
        }
    }
    swap(arr[i + 1], arr[high]);
    return i + 1;
}
```

---

## 12. 编译器支持

### 12.1 C++ 标准

三元运算符在所有 C++ 标准中都支持（C++98/03/11/14/17/20/23）。

### 12.2 编译器优化

现代编译器（GCC、Clang、MSVC）对三元运算符有很好的优化：
- 可能生成 CMOV（条件移动）指令
- 避免分支预测失败
- 与 if-else 性能相当或更好

---

## 13. 替代方案

### 13.1 C++17: std::clamp

```cpp
#include <algorithm>

// 替代三元运算符
int value = std::clamp(input, min, max);

// 等价于
int value = (input < min) ? min : (input > max) ? max : input;
```

### 13.2 C++17: if constexpr

```cpp
template<typename T>
void process(T value) {
    if constexpr (std::is_integral_v<T>) {
        // 整数类型处理
    } else {
        // 其他类型处理
    }
}
```

### 13.3 std::max / std::min

```cpp
#include <algorithm>

int max_value = std::max(a, b);
int min_value = std::min(a, b);

// 替代
int max_value = (a > b) ? a : b;
int min_value = (a < b) ? a : b;
```

---

## 14. 常见面试题

### 14.1 什么是三元运算符？

**答**：三元运算符是 C++ 中唯一的三目运算符，语法为 `条件 ? 表达式1 : 表达式2`。当条件为真时返回表达式1，否则返回表达式2。

### 14.2 三元运算符和 if-else 有什么区别？

**答**：
1. 三元运算符是**表达式**，有返回值；if-else 是**语句**
2. 三元运算符更简洁，适合简单条件
3. if-else 更适合复杂逻辑和多语句执行
4. 性能上现代编译器会优化，基本没有差异

### 14.3 能否嵌套使用三元运算符？

**答**：可以，但不推荐过度嵌套，会降低可读性。嵌套层数超过 2 层时建议使用 if-else。

### 14.4 三元运算符的优先级是多少？

**答**：三元运算符的优先级很低，仅高于赋值运算符和逗号运算符。建议使用括号明确优先级。

---

## 15. 总结

### 核心要点

1. **语法**：`条件 ? 表达式1 : 表达式2`
2. **用途**：简洁的条件赋值和表达式选择
3. **优点**：代码简洁、可读性好（简单情况）
4. **缺点**：嵌套时难读、不适合复杂逻辑
5. **建议**：简单条件用三元，复杂逻辑用 if-else

### 使用准则

| 场景 | 推荐 |
|------|------|
| 简单条件赋值 | ✅ 三元运算符 |
| 返回值选择 | ✅ 三元运算符 |
| 函数参数 | ✅ 三元运算符 |
| 2+ 层嵌套 | ❌ 使用 if-else |
| 多语句执行 | ❌ 使用 if-else |
| 复杂逻辑 | ❌ 使用 if-else |
| 需要调试 | ❌ 使用 if-else |

### 记忆口诀

> **简单条件三元好，复杂逻辑 if 为妙**

### 最佳实践

```cpp
// ✅ 好的用法
int max = (a > b) ? a : b;
string status = (age >= 18) ? "Adult" : "Minor";
return (x > 0) ? x : -x;

// ❌ 不好的用法
int result = (a > b) ? ((c > d) ? 1 : 2) : ((e > f) ? 3 : 4);  // 太复杂
(condition) ? (doA(), doB(), x) : (doC(), doD(), y);  // 有副作用
```

**最重要的原则：代码可读性 > 简洁性 > 性能** 🎯