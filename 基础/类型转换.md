# float 或 double 转换为 int 时，会**直接截断小数部分**，而不是四舍五入。

```c
int x = (int)3.14;
```

这里的 (int) 是强制类型转换（type casting），结果就是把 3.14 变成整数 3

# 📝 大小写转换

### ✅ 标准函数（推荐）：

| 函数         | 说明        | 示例                  |
| ---------- | --------- | ------------------- |
| toupper(c) | 把字符       | toupper('a') → 'A'  |
| tolower(c) | 把字符       | tolower('G') → 'g'  |
| islower(c) | 判断是否为小写字母 | islower('x') → true |
| isupper(c) | 判断是否为大写字母 | isupper('X') → true |

> 💡 这些函数需要头文件：#include <cctype>

### ✅ 推荐写法（示例）：

```cpp
#include <iostream>
#include <cctype>  // 包含 islower, isupper, toupper, tolower
using namespace std;

int main() {
    char c;
    cin >> c;

    if (islower(c)) cout << (char)toupper(c) << endl;
    else if (isupper(c)) cout << (char)tolower(c) << endl;
}
```

# 🧠 数字与大小写字母转换

### ✅ 字母 → 数字（索引）

```cpp
char c1 = 'a';
int index1 = c1 - 'a';   // 0

char c2 = 'b';
int index2 = c2 - 'a';   // 1

char c3 = 'z';
int index3 = c3 - 'a';   // 25
```

| 字符  | 表达式       | 结果  |
| --- | --------- | --- |
| 'a' | 'a' - 'a' | 0   |
| 'b' | 'b' - 'a' | 1   |
| 'z' | 'z' - 'a' | 25  |

### ✅ 数字 → 字母（字符）

```cpp
int i1 = 0;
char ch1 = i1 + 'a';     // 'a'

int i2 = 1;
char ch2 = i2 + 'a';     // 'b'

int i3 = 25;
char ch3 = i3 + 'a';     // 'z'
```

| 数字  | 表达式      | 字符  |
| --- | -------- | --- |
| 0   | 'a' + 0  | 'a' |
| 1   | 'a' + 1  | 'b' |
| 25  | 'a' + 25 | 'z' |

字母减a得索引，数组记录最稳定；

输出时加回a，频率统计效率顶；

# 🔁 int ➜ string

```cpp
int num = 1234;
string s = to_string(num);
```

# 🔁 string ➜ int

```cpp
string s = "5678";
int num = stoi(s);  // string to int
```

# 字符转数字

```cpp
for (char ch : num_str) {
    sum += ch - '0';  // 字符转数字
}
```