# bool 是一种 **布尔类型**，它的作用是：

> 用来表示 **逻辑值**，即 **“真” 或 “假”**（true 或 false）。

🧠 它的作用总结一句话：

> **用来写判断、逻辑条件、返回真假结果**，让代码更清晰、可读性更强。

bool 在 C 语言里不是原生内建类型，必须：

```cpp
#include <stdbool.h>
```

🔍 举个例子：

```c
#include <stdio.h>
#include <stdbool.h>

bool is_even(int n) {
    return n % 2 == 0;
}

int main() {
    int n = 6;
    if (is_even(n)) {
        printf("n 是偶数\n");
    } else {
        printf("n 是奇数\n");
    }
    return 0;
}
```

is_ 这个前缀 **不是必须写的**，它只是一个 **命名习惯**，不是语法规定。

# 返还.size()

```cpp
return digits.size() == 4;
//判断 digits 这个集合中是否有 4 个不同的字符。
//如果是 "2013"：set 里面是 {'0', '1', '2', '3'} → 有 4 个不同字符 ✔️
//如果是 "2020"：set 是 {'0', '2'} → 只有 2 个不同字符 ❌
```

# 输出bool

### 默认情况下：

```cpp
bool flag = true;
cout << flag << endl;
```

输出的是：1

```cpp
bool flag = false;
cout << flag << endl;
```

输出的是：0

### 如果想输出 true 或 false 这两个字样，而不是 1 和 0：

可以用 boolalpha：

```cpp
bool flag = true;
cout << boolalpha << flag << endl;  // 输出 true

flag = false;
cout << boolalpha << flag << endl;  // 输出 false
```