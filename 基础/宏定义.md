```cpp
//0 based
#define all(a) a.begin(), a.end()
#define rall(a) a.rbegin(), a.rend()

//1 based
#define all1(a) a.begin() + 1, a.end()
#define rall1(a) a.rbegin(), a.end() - 1

//数组中最大值的索引
int index = max_element(all(a)) - a.begin();
int index = max_element(all1(a)) - a.begin();
```

