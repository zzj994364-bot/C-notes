```cpp
#include <iostream>
#include <vector>
using namespace std;
vector<vector<int>> graph;
vector<bool> visited;
vector<int> all_subordinates;

//直接统计all_subordinates
int dfs(int u) {
    int total = 0;
    visited[u] = true;
    //遍历所有邻接点
    for (auto v : graph[u]) {
        //确保访问子节点
        if (!visited[v]) {
            int subtree_size = dfs(v);
            total += subtree_size;
        }
    }
    all_subordinates[u] = total;
    return total + 1;
}

int main() {
    int n;
    cin >> n;
    graph.resize(n + 1);
    visited.resize(n + 1, false);
    all_subordinates.resize(n + 1);
    //adjacency list
    for (int a = 2; a <= n; a++) {
        int b;
        cin >> b;
        graph[a].push_back(b);
        graph[b].push_back(a);
    }
    dfs(1);
    for (int i = 1; i <= n; i++) {
        cout << all_subordinates[i] << ' ';
    }
    return 0;
}

/*
=============================================================================
                        DFS递归流程图（示例输入 n=7）
=============================================================================

假设输入构建的树结构为：
        1 (根节点)
       /|\
      2 3 4
     /|   |
    5 6   7

输入数据：
7
1 1 1 2 2 4
(表示: 2的父节点是1, 3的父节点是1, 4的父节点是1, 5的父节点是2, 6的父节点是2, 7的父节点是4)

-----------------------------------------------------------------------------
递归调用树：
-----------------------------------------------------------------------------

                        dfs(1)
                          |
        visited[1]=true, total=0
                          |
        +--------+--------+--------+
        |        |        |        |
    遍历邻居: 2    3        4     (假设顺序)
        |        |        |
        v        v        v
    dfs(2)   dfs(3)   dfs(4)
        |        |        |
-----------------------------------------------------------------------------

详细执行流程：

┌─ dfs(1) ────────────────────────────────────────────────┐
│ visited[1] = true                                        │
│ total = 0                                                │
│                                                          │
│ ┌─ 遍历邻居 2 ──────────────────────────────────────┐   │
│ │ !visited[2] = true → 递归调用                      │   │
│ │                                                    │   │
│ │ ┌─ dfs(2) ────────────────────────────────────┐  │   │
│ │ │ visited[2] = true                           │  │   │
│ │ │ total = 0                                   │  │   │
│ │ │                                             │  │   │
│ │ │ ┌─ 遍历邻居 1 ─────────────────────────┐   │  │   │
│ │ │ │ visited[1] = true, 跳过              │   │  │   │
│ │ │ └──────────────────────────────────────┘   │  │   │
│ │ │                                             │  │   │
│ │ │ ┌─ 遍历邻居 5 ─────────────────────────┐   │  │   │
│ │ │ │ !visited[5] = true → 递归调用        │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ ┌─ dfs(5) ────────────────────────┐ │   │  │   │
│ │ │ │ │ visited[5] = true               │ │   │  │   │
│ │ │ │ │ total = 0                       │ │   │  │   │
│ │ │ │ │ 遍历邻居 2: visited[2]=true跳过 │ │   │  │   │
│ │ │ │ │ all_subordinates[5] = 0         │ │   │  │   │
│ │ │ │ │ return 0 + 1 = 1                │ │   │  │   │
│ │ │ │ └─────────────────────────────────┘ │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ subtree_size = 1                     │   │  │   │
│ │ │ │ total = 0 + 1 = 1                    │   │  │   │
│ │ │ └───────────────────────────────────────┘   │  │   │
│ │ │                                             │  │   │
│ │ │ ┌─ 遍历邻居 6 ─────────────────────────┐   │  │   │
│ │ │ │ !visited[6] = true → 递归调用        │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ ┌─ dfs(6) ────────────────────────┐ │   │  │   │
│ │ │ │ │ visited[6] = true               │ │   │  │   │
│ │ │ │ │ total = 0                       │ │   │  │   │
│ │ │ │ │ 遍历邻居 2: visited[2]=true跳过 │ │   │  │   │
│ │ │ │ │ all_subordinates[6] = 0         │ │   │  │   │
│ │ │ │ │ return 0 + 1 = 1                │ │   │  │   │
│ │ │ │ └─────────────────────────────────┘ │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ subtree_size = 1                     │   │  │   │
│ │ │ │ total = 1 + 1 = 2                    │   │  │   │
│ │ │ └───────────────────────────────────────┘   │  │   │
│ │ │                                             │  │   │
│ │ │ all_subordinates[2] = 2                    │  │   │
│ │ │ return 2 + 1 = 3                           │  │   │
│ │ └─────────────────────────────────────────────┘  │   │
│ │                                                    │   │
│ │ subtree_size = 3                                  │   │
│ │ total = 0 + 3 = 3                                 │   │
│ └────────────────────────────────────────────────────┘   │
│                                                          │
│ ┌─ 遍历邻居 3 ──────────────────────────────────────┐   │
│ │ !visited[3] = true → 递归调用                      │   │
│ │                                                    │   │
│ │ ┌─ dfs(3) ────────────────────────────────────┐  │   │
│ │ │ visited[3] = true                           │  │   │
│ │ │ total = 0                                   │  │   │
│ │ │ 遍历邻居 1: visited[1]=true, 跳过           │  │   │
│ │ │ all_subordinates[3] = 0                     │  │   │
│ │ │ return 0 + 1 = 1                            │  │   │
│ │ └─────────────────────────────────────────────┘  │   │
│ │                                                    │   │
│ │ subtree_size = 1                                  │   │
│ │ total = 3 + 1 = 4                                 │   │
│ └────────────────────────────────────────────────────┘   │
│                                                          │
│ ┌─ 遍历邻居 4 ──────────────────────────────────────┐   │
│ │ !visited[4] = true → 递归调用                      │   │
│ │                                                    │   │
│ │ ┌─ dfs(4) ────────────────────────────────────┐  │   │
│ │ │ visited[4] = true                           │  │   │
│ │ │ total = 0                                   │  │   │
│ │ │                                             │  │   │
│ │ │ ┌─ 遍历邻居 1 ─────────────────────────┐   │  │   │
│ │ │ │ visited[1] = true, 跳过              │   │  │   │
│ │ │ └──────────────────────────────────────┘   │  │   │
│ │ │                                             │  │   │
│ │ │ ┌─ 遍历邻居 7 ─────────────────────────┐   │  │   │
│ │ │ │ !visited[7] = true → 递归调用        │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ ┌─ dfs(7) ────────────────────────┐ │   │  │   │
│ │ │ │ │ visited[7] = true               │ │   │  │   │
│ │ │ │ │ total = 0                       │ │   │  │   │
│ │ │ │ │ 遍历邻居 4: visited[4]=true跳过 │ │   │  │   │
│ │ │ │ │ all_subordinates[7] = 0         │ │   │  │   │
│ │ │ │ │ return 0 + 1 = 1                │ │   │  │   │
│ │ │ │ └─────────────────────────────────┘ │   │  │   │
│ │ │ │                                       │   │  │   │
│ │ │ │ subtree_size = 1                     │   │  │   │
│ │ │ │ total = 0 + 1 = 1                    │   │  │   │
│ │ │ └───────────────────────────────────────┘   │  │   │
│ │ │                                             │  │   │
│ │ │ all_subordinates[4] = 1                    │  │   │
│ │ │ return 1 + 1 = 2                           │  │   │
│ │ └─────────────────────────────────────────────┘  │   │
│ │                                                    │   │
│ │ subtree_size = 2                                  │   │
│ │ total = 4 + 2 = 6                                 │   │
│ └────────────────────────────────────────────────────┘   │
│                                                          │
│ all_subordinates[1] = 6                                  │
│ return 6 + 1 = 7                                         │
└──────────────────────────────────────────────────────────┘

=============================================================================
最终结果：
=============================================================================
all_subordinates[1] = 6  (节点1的所有下属: 2,3,4,5,6,7)
all_subordinates[2] = 2  (节点2的所有下属: 5,6)
all_subordinates[3] = 0  (节点3无下属)
all_subordinates[4] = 1  (节点4的下属: 7)
all_subordinates[5] = 0  (节点5无下属)
all_subordinates[6] = 0  (节点6无下属)
all_subordinates[7] = 0  (节点7无下属)

输出: 6 2 0 1 0 0 0

=============================================================================
关键理解：
=============================================================================
1. 返回值含义: dfs(u) 返回以u为根的子树的节点总数（包括u自己）
2. total统计: 累加所有子节点的子树大小
3. all_subordinates[u]: 只统计下属数量（不包括u自己），即total
4. 后序遍历: 先递归处理所有子节点，再统计当前节点
5. visited数组: 防止无向图中的重复访问（父子相互连接）
*/
```

